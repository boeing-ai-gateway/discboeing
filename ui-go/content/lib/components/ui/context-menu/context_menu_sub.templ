package contextmenu

import ahtempl "github.com/a-h/templ"

templ ContextMenuSub(open bool, children ahtempl.Component) {
	<div data-slot="context-menu-sub" data-state={ state(open) }>
		if children != nil {
			@children
		}
	</div>
}

package contextmenu

import "github.com/obot-platform/discobot/ui-go/content/lib/classnames"
import ahtempl "github.com/a-h/templ"

templ ContextMenuGroup(className string, children ahtempl.Component) {
	<div data-slot="context-menu-group" role="group" class={ classnames.CN(className) }>
		if children != nil {
			@children
		}
	</div>
}

package alertdialog

import ahtempl "github.com/a-h/templ"

templ AlertDialogTitle(className string, children ahtempl.Component) {
	<div data-slot="alert-dialog-title" class={ joinClass("text-lg font-semibold", className) }>
		if children != nil {
			@children
		}
	</div>
}

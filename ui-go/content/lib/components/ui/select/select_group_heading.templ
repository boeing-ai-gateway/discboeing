package selectui

import ahtempl "github.com/a-h/templ"

templ SelectGroupHeading(className string, children ahtempl.Component) {
	<div data-slot="select-group-heading" class={ labelClass(className) }>
		if children != nil {
			@children
		}
	</div>
}

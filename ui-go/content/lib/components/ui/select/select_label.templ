package selectui

import ahtempl "github.com/a-h/templ"

templ SelectLabel(className string, children ahtempl.Component) {
	<div data-slot="select-label" class={ labelClass(className) }>
		if children != nil {
			@children
		}
	</div>
}

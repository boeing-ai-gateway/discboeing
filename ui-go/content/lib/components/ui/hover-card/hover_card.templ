package hovercard

import ahtempl "github.com/a-h/templ"

templ HoverCard(open bool, children ahtempl.Component) {
	<div data-slot="hover-card" data-state={ state(open) }>
		if children != nil {
			@children
		}
	</div>
}

package tooltip

import ahtempl "github.com/a-h/templ"

templ TooltipTrigger(className string, children ahtempl.Component) {
	<button type="button" data-slot="tooltip-trigger" class={ joinClass(className) }>
		if children != nil {
			@children
		}
	</button>
}

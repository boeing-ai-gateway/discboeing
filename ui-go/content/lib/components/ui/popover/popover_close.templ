package popover

import "github.com/obot-platform/discobot/ui-go/content/lib/classnames"
import ahtempl "github.com/a-h/templ"

templ PopoverClose(className string, children ahtempl.Component) {
	<button type="button" data-slot="popover-close" class={ classnames.CN(className) }>
		if children != nil {
			@children
		}
	</button>
}

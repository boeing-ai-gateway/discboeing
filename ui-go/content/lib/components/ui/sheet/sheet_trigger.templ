package sheet

import ahtempl "github.com/a-h/templ"

templ SheetTrigger(className string, children ahtempl.Component) {
	<button type="button" data-slot="sheet-trigger" class={ joinClass(className) }>
		if children != nil {
			@children
		}
	</button>
}

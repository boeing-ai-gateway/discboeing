package sheet

import "github.com/obot-platform/discobot/ui-go/content/lib/classnames"
import ahtempl "github.com/a-h/templ"

templ SheetTrigger(className string, children ahtempl.Component) {
	<button type="button" data-slot="sheet-trigger" class={ classnames.CN(className) }>
		if children != nil {
			@children
		}
	</button>
}

package sheet

import ahtempl "github.com/a-h/templ"

templ SheetTitle(className string, children ahtempl.Component) {
	<h2 data-slot="sheet-title" class={ titleClass(className) }>
		if children != nil {
			@children
		}
	</h2>
}

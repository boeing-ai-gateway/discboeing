package item

import ahtempl "github.com/a-h/templ"

templ ItemActions(className string, children ahtempl.Component) {
	<div data-slot="item-actions" class={ joinClass("flex items-center gap-2", className) }>
		if children != nil {
			@children
		}
	</div>
}

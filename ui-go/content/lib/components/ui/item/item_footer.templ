package item

import ahtempl "github.com/a-h/templ"

templ ItemFooter(className string, children ahtempl.Component) {
	<div data-slot="item-footer" class={ joinClass("flex basis-full items-center justify-between gap-2", className) }>
		if children != nil {
			@children
		}
	</div>
}

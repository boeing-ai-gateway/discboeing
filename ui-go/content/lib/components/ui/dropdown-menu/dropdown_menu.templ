package dropdownmenu

import ahtempl "github.com/a-h/templ"

templ DropdownMenu(open bool, children ahtempl.Component) {
	<div data-slot="dropdown-menu" data-state={ state(open) }>
		if children != nil {
			@children
		}
	</div>
}

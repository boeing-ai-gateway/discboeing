package dropdownmenu

import ahtempl "github.com/a-h/templ"

templ DropdownMenuTrigger(className string, children ahtempl.Component) {
	<button type="button" data-slot="dropdown-menu-trigger" class={ joinClass(className) }>
		if children != nil {
			@children
		}
	</button>
}

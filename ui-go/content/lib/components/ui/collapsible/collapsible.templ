package collapsible

import ahtempl "github.com/a-h/templ"

templ Collapsible(open bool, className string, children ahtempl.Component) {
	<div data-slot="collapsible" data-state={ collapsibleState(open) } class={ joinClass(className) }>
		if children != nil {
			@children
		}
	</div>
}

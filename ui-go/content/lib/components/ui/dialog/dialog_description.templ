package dialog

import ahtempl "github.com/a-h/templ"

templ DialogDescription(className string, children ahtempl.Component) {
	<p data-slot="dialog-description" class={ descriptionClass(className) }>
		if children != nil {
			@children
		}
	</p>
}

package dialog

import ahtempl "github.com/a-h/templ"

templ DialogTrigger(className string, children ahtempl.Component) {
	<button type="button" data-slot="dialog-trigger" class={ joinClass(className) }>
		if children != nil {
			@children
		}
	</button>
}

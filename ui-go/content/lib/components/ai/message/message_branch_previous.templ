package message

import (
	ahtempl "github.com/a-h/templ"
	lucide "github.com/bryanvaz/go-templ-lucide-icons/icons"
)

templ MessageBranchPrevious(branch BranchView, children ahtempl.Component) {
	<button aria-label="Previous branch" disabled?={ branchTotal(branch) <= 1 } type="button" class={ branchButtonClass("") } data-branch-action="previous" data-on:click={ branchCommand(branch.MessageID, "previous") }>
		if children != nil {
			@children
		} else {
			@lucide.ChevronLeft(ahtempl.Attributes{"class": "size-3.5"})
		}
	</button>
}

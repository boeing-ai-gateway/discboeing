package codeblock

import ahtempl "github.com/a-h/templ"
import "github.com/obot-platform/discobot/ui-go/content/lib/classnames"

templ CodeBlockActions(className string, children ahtempl.Component) {
	<div class={ classnames.CN("flex items-center gap-2", className) }>
		if children != nil {
			@children
		}
	</div>
}
